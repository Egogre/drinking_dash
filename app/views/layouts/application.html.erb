<!DOCTYPE html>
<html>
<head>
  <meta charset= "utf-8">
  <title>Drinking Dash</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <header id="page_header" role="banner">
    <nav id="utility">
      <div id="login">
        <% if signed_in? %>
          <li>
            <%= link_to "Users", '#' %>
          </li>
          <li>
            <%= link_to "Profile", current_user %>
          </li>
          <li>
            <%= link_to "Sign Out", signout_path, method: "delete" %>
          </li>
        <% else %>
          <%= render 'sessions/form' %>
        <% end %>
      </div>
    </nav>
  <%= link_to(image_tag("logo.png", alt: "Logo", id: "logo"), root_url, title: "Dashboard") %>
  </header>
  <nav id="links">
    <ul>
      <li><%= link_to 'Menu', categories_path %></li>
      <li><%= link_to 'Home', root_path %></li>
      <!-- <li><%= link_to 'Contact', root_path %></li>           -->
    </ul>
  </nav>
  <div class="container">
    <section id="content">
      <p class="flash"><%= flash.notice %></p>
      <%= yield %>
    </section>
    <section id="sidebar">
      <section id="recent_activity">
        <header><h1 class="ir" id="recent">Shopping Cart</h1></header>
	<ul>
	<% current_order.order_items.each do |item| %>
	  <li> <%= item.drink.name %>: <%= number_to_currency item.drink.price %></li>
	<% end %>
	</ul>
      </section>
    </section>
  </div>
  <%= render 'layouts/footer' %>
</body>
</html>







